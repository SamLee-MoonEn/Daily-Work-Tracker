import { styled } from "styled-components";
import tw from "twin.macro";

export default function LoginPage() {
  return <MainContainer>로그인</MainContainer>;
}

const MainContainer = styled.div`
  ${tw`w-11/12 ml-auto`}
`;
